from Program import *
class Cpp(Program):
	def __init__(self, usn, course, assign, file_path, files):
		Program.__init__(self, usn, course, assign, file_path, files);

	def display(self):
		Program.display(self);
